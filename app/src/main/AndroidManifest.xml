<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="my.edu.tarc.proin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity" />
        <activity android:name=".TopActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".ServicesActivity" />
        <activity android:name=".CartActivity"></activity>
        <activity
            android:name=".ProductActivity"
            android:label="Critical Illness "
            android:parentActivityName=".HomeActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ImageActivity" />
        <activity android:name=".SettingActivity"/>
        <activity android:name=".EditProfileActivity" android:label="EDIT DETAILS" android:parentActivityName=".SettingActivity">

        </activity>
    </application>

</manifest>